.env
__pycache__/
*.pyc
*.pyo

# SQLite data (persisted on Railway volume, not in git)
beat_battle.db

# Legacy JSON data files + post-migration backups (not in git)
users.json
battles.json
queue.json
finals.json
settings.json
*.json.bak
